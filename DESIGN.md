# f7goods — 日式清透风设计规范

## 定位

永远的7日之都同人周边展示平台。页面职责：清晰浏览、筛选、发现周边与作者。受众是同人爱好者与创作者，不是电商买家——不做促销感，做「同人志贩卖会目录」的安静与可信。

## 风格锚点

参考：日本同人志即卖会场刊 / 轻小说文库封面排版 / 无印良品纸质品。不是赛博、不是电商促销、不是 Material 模板。

关键词：washi 纸感、细线、朱印式点缀、大留白、衬线标题 + 无衬线正文。

## 色板

| Token | 值 | 用途 |
|---|---|---|
| `--ink` | `#22201E` | 主文字、页脚底（暖近黑，不用冷藏青） |
| `--paper` | `#F6F2EB` | 页面底色，washi 暖纸 |
| `--card` | `#FFFCF7` | 卡片面 |
| `--accent` | `#D94F5C` | 朱色强调：价格、active、喜爱 |
| `--accent-dark` | `#B83D49` | 按钮 hover |
| `--accent-alt` | `#2C3340` | 次强调（语言 active、确认键），用量极少 |
| `--muted` | `#8A8078` | 次要文字（原 `--haze`） |
| `--line` | `#E5DDD1` | 发丝线边框（原 `--border`） |
| `--glass` | `rgba(246,242,235,0.88)` | 导航毛玻璃，偏纸不偏白 |
| `--success` | `#3D8B6E` | 成功 |
| `--warning` | `#C48A2A` | 预约/进行中 |

原则：朱红只做点睛（价格、active 胶囊、喜爱态、截止角标），不用大面积渐变。去掉 `accent`→`accent-alt` 的红蓝双色渐变条。

## 字体

- 展示：`'Noto Serif SC', Georgia, 'Times New Roman', serif` — 品牌、Hero、页标题、卡片标题、活动标题
- 正文：`'Noto Sans SC', 'Segoe UI', system-ui, sans-serif` — UI、正文、元信息
- 字阶：Hero `clamp(1.75rem, 4vw, 2.5rem)`；页标题 `1.5rem`；卡片标题 `1rem`；正文 `0.95rem/1.75`；元信息 `0.8rem`
- 字重：标题 600–700，正文 400，按钮/标签 500–600

## 布局

- 内容宽 `1120px`，两侧 `1.5rem` 边距
- 章节间距：页头下 `2rem`，筛选下 `1.5rem`，网格 gap `1.25rem`
- 卡片网格：`auto-fill, minmax(260px, 1fr)`；移动端 2 列
- 圆角：卡片 `14px`，控件 `10px`，胶囊 `999px`
- 阴影：默认几乎无影，hover 仅 `0 8px 24px rgba(34,32,30,0.08)` + 边框加深

## 标志性处理

1. **纸面感**：暖纸底 + 暖白卡片 + 发丝线，不用重阴影浮起
2. **朱印点缀**：品牌条改为 2px 朱色细线；active 状态是朱色胶囊，不是大块红底
3. **安静 Hero**：去掉深色大渐变，改为纸面编辑式页头——细朱线上方，大衬线标题左对齐，副题次级灰
4. **筛选一体**：视觉上收敛为工具条，胶囊按钮更小、更整齐，active 朱色实心

## 禁止

- 红→藏青品牌渐变条
- Hero 深色大渐变 + 白字居中
- 卡片大幅上浮 + 红边高亮
- 主流程 UI 依赖 emoji 图标（空状态可保留）
- 多层 filter-bar 无分隔堆叠的观感（结构可保留，样式必须像一套工具条）

## 组件规格

### 导航
- 高 60px，纸色毛玻璃，底 1px `--line`
- 顶 2px `--accent` 细线（替代渐变 brand-bar）
- 链接：次级灰，hover/active 朱色字 + 极淡朱底
- 作者入口：朱色实心胶囊，小号

### 卡片
- 暖白底、1px `--line`、`14px` 圆角
- 图 180–200px cover；无图用暖灰占位
- 状态标签：小胶囊，按状态着色（发售中=朱、预约=琥珀、售罄=灰、即将=墨）
- 底栏：左价格（朱、半粗），右元信息（想要/日期/喜爱）次级灰
- hover：边框色变 `#D4C8B8`，位移 `-2px`，轻阴影

### 筛选
- 胶囊：高约 32px，字 0.8rem，未选纸底细线，选中朱实心
- 搜索/排序与胶囊同高同圆角，右侧对齐

### 按钮
- primary：朱实心，hover 加深，无渐变
- default：纸底细线
- focus-visible：2px 朱色外环

### 页脚
- `--ink` 底，文字 0.72 白，链接 hover 朱色
- 四列 → 移动单列；ICP 行更安静

## 动效

- 页面入场 0.28s opacity + 6px 上移
- 卡片/按钮 transition 0.2s ease
- 尊重 `prefers-reduced-motion`

## App Shell（现行布局）

首页/列表页使用左侧固定 rail（72px）+ 内容区。rail **必须**是纸色 `--card` + 1px `--line`，不可近黑。轮播/主推块为暖白卡片 + 顶 2px 朱线；有封面图时才允许深色遮罩保证白字可读。

## 暗色模式

跟随系统 `prefers-color-scheme: dark`，token 在 `style.css` 末段覆盖：paper 深暖灰、card 略亮、accent 提亮朱色。禁止另起冷蓝主题。

